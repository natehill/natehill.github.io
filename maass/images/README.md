# README

- Copy Image files into this folder
- Open a terminal and navigate to this folder
- run the following command `ls -1 | grep -v "images.txt" > images.txt` . This will create a list of files that we can then read on the main index.html to generate the flipbook
- Upload everything to a page/somewhere
